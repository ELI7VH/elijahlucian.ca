export const Content = () => {
  // either media or text
  // adapters calling adapters.
  // layer text over media
  // it is text and media.. both. either.

  // layers: (Text,Media,Post,Comment,Reference)[]
  // this project relaly is all about layers.
  // ui is made of layers
  // content is live layered in the platform.

  return <div></div>
}
