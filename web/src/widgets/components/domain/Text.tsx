type Props = {
  text: string
  comments: string[] // comments is a module that is rendered in children maybe? we are going completely stateless...
}

export const Text = ({ text }: Props) => {
  //
  return <div>Text</div>
}
