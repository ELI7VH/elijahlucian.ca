export const FileGrabbr = () => {
  // file drag and drop
  // or
  // ctrl v
  // detects text or image or video or audio or link
  // selects upload component, navigates to it, inserting payload into the component -> handles rest

  return <div>FileGrabbr</div>
}
