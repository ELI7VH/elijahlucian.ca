export const DataSender = () => {
  // a simple tool that helps you migrate data between environments or systems
  // it sends records, and any associated files
  // attempts to preserve id names, and if not generates a backwards compatible mapping

  return <div>DataSender</div>
}
