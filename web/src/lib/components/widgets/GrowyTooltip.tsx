export const GrowyTooltip = () => {
  // tooltip
  // when hovers children, it slowly grows out, when it reaches a screen boundary, it adds growth to other side

  // ideas: calculate content area minimums nad grow as needed.
  // could also use for a dynamic destkop thing that integreats all cool
  // like wraps around corners of other things...

  return <div>GrowyTooltip</div>
}
